ln -sf . compiler

./mudlle <<-!
	garbage_collect(1024*1024)
	load("xc.mud")
	load("pxslow.mud")
!
[ "$?" = 0 ] || exit 1

./mudlle <<-!
	garbage_collect(1024*1024)
	load("icxc.mud")
	load("pxslow.mud")
!
[ "$?" = 0 ] || exit 1
